# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact - Ma Boutique</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1>Contact</h1>
    <nav>
      <a href="index.html">Accueil</a>
      <a href="panier.html">Panier</a>
    </nav>
  </header
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:let panier = JSON.parse(localStorage.getItem('panier')) || [];

function ajouterAuPanier(nom, prix) {
  panier.push({nom, prix});
  localStorage.setItem('panier', JSON.stringify(panier));
  alert(nom + " a été ajouté au panier !");
}
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this wbody {
  font-family: 'Arial', sans-serif;
  margin: 0;
  background-color: #f2f2f2;
}

header {
  background-color: #111;
  color: #fff;
  padding: 15px;
  text-align: center;
}

header nav a {
  color: white;
  margin: 0 15px;
  text-decoration: none;
  font-weight: bold;
}

header nav a:hover {
  text-decoration: underline;
}

.produits {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 25px;
  padding: 30px;
}

.produit {
  background-color: white;
  padding: 20px;
  border-radius: 12px;
  width: 220px;
  text-align: center;
  box-shadow: 0 5px 15px rgba(0,0,0,0.1);
  transition: transform 0.2s;
}

.produit:hover {
  transform: scale(1.05);
}

.produit img {
  width: 100%;
  border-radius: 8px;
}

button {
  margin-top: 10px;
  background-color: #ff6600;
  border: none;
  color:orkflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job c<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ma Boutique</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1>Ma Boutique</h1>
    <nav>
      <a href="index.html">Accueil</a>
      <a href="panier.html">Panier</a>
      <a href="contact.html">Contact</a>
    </nav>
  </header>

  <main>
    <section class="produits">
      <div class="produit">
        <img src="images/produit1.jpg" alt="Produit 1">
        <h2>Produit 1</h2>
        <p>Prix : 20€</p>
        <button onclick="ajouterAuPanier('Produit 1', 20)">Ajouter au panier</button>
      </div>

      <div class="produit">
        <img src="images/produit2.jpg" alt="Produit 2">
        <h2>Produit 2</h2>
        <p>Prix : 35€</p>
        <button onclick="ajouterAuPanier('Produit 2', 35)">Ajouter au panier</button>
      </div>

      <div class="produit">
        <img src="images/produit3.jpg" alt="Produit 3">
        <h2>Produit 3</h2>
        <p>Prix : 50€</p>
        <button onclick="ajouterAuPanier('Produit 3', 50)">Ajouter au panier</button>
      </div>
    </section>
  </main>

  <footer>
    <p>© 2026 Ma Boutique</p>
  </footer>

  <script src="script.js"></script>
</body>
</html>alled "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks thlet panier = JSON.parse(localStorage.getItem('panier')) || [];

function ajouterAuPanier(nom, prix) {
  panier.push({nom, prix});
  localStorage.setItem('panier', JSON.stringify(panier));
  alert(nom + " a été ajouté au panier !");
}at will be executed as part of the job
    steps:
      # Checks-out your repository under $Gbody {
  font-family: 'Arial', sans-serif;
  margin: 0;
  background-color: #f2f2f2;
}

header {
  background-color: #111;
  color: #fff;
  padding: 15px;
  text-align: center;
}

header nav a {
  color: white;
  margin: 0 15px;
  text-decoration: none;
  font-weight: bold;
}

header nav a:hover {
  text-decoration: underline;
}

.produits {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 25px;
  padding: 30px;
}

.produit {
  background-color: white;
  padding: 20px;
  border-radius: 12px;
  width: 220px;
  text-align: center;
  box-shadow: 0 5px 15px rgba(0,0,0,0.1);
  transition: transform 0.2s;
}

.produit:hover {
  transform: scale(1.05);
}

.produit img {
  width: 100%;
  border-radius: 8px;
}

button {
  margin-top: 10px;
  background-color: #ff6600;
  border: none;
  color:ITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
